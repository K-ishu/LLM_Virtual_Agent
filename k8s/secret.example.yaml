apiVersion: v1
kind: Secret
metadata:
  name: llm-se-assistant-secrets
  namespace: llm-se-assistant
type: Opaque
stringData:
  # Replace this value in a real cluster.
  # Do not commit real API keys.
  OPENAI_API_KEY: "replace-with-real-key"
