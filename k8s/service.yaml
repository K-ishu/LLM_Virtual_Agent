apiVersion: v1
kind: Service
metadata:
  name: llm-se-assistant
  namespace: llm-se-assistant
  labels:
    app.kubernetes.io/name: llm-se-assistant
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: llm-se-assistant
  ports:
    - name: http
      port: 80
      targetPort: http
