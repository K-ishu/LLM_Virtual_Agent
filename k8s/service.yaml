apiVersion: v1
kind: Service
metadata:
  name: llm-se-assistant
  namespace: llm-se-assistant
  labels:
    app: llm-se-assistant
spec:
  type: ClusterIP
  selector:
    app: llm-se-assistant
  ports:
    - name: http
      port: 80
      targetPort: 8501
